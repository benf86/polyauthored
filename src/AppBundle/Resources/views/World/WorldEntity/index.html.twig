{% extends '::base.html.twig' %}

{% block body -%}
    <h1>World\WorldEntity list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>Content</th>
                <th>Approved</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('world_entity_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.type }}</td>
                <td>{{ entity.content }}</td>
                <td>{{ entity.approved }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('world_entity_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('world_entity_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('world_entity_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
